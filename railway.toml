[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash /var/www/docker/php/start.sh && /usr/bin/supervisord -n -c /etc/supervisor/conf.d/supervisord.conf"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3